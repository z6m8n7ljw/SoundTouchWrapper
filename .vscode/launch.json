{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/main",
            "args": ["${workspaceFolder}/input.wav", "${workspaceFolder}/wav/output.wav", "-pitch=-4", "-speech"],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "build"
        }
    ]
}
